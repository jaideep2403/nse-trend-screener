__pycache__/
*.pyc
*.pyo
*.pyd
.env
.venv
venv/
*.egg-info/
dist/
build/
.DS_Store
/tmp/
*.log
nohup.out

# Runtime data — generated by the app at startup, never committed
fundamentals.db
stage_transitions.db
momentum_freshness.db
*.db
.stock_metrics.pkl
.nse_universe_cache.pkl
.nifty50_cache.pkl
.breadth_cache.pkl
.breadth_score_history.json
.breadth_calibration.json
.scan_cache/

# IDE / agent runtime metadata — local-only, per-developer
.claude/

# LOCAL-ONLY: Personal Portfolio feature — never pushed to GitHub
portfolio.py
templates/portfolio_tab.html
static/portfolio.js
.portfolio.json

# LOCAL-ONLY: Investment Grade scanner — never pushed to GitHub
# investment_grade.py   # un-ignored 2026-08-04: needed by the PUBLIC site
# templates/investment_grade_tab.html   # un-ignored 2026-08-04: needed by the PUBLIC site
# static/investment_grade.js   # un-ignored 2026-08-04: needed by the PUBLIC site

# LOCAL-ONLY: hand-curated extra symbols outside NSE indices (e.g. PRECWIRE)
.extra_symbols.json

# LOCAL-ONLY: Alpha Engine — institutional multi-factor composite scorer
# alpha_engine.py   # un-ignored 2026-08-04: needed by the PUBLIC site
# fo_data.py   # un-ignored 2026-08-04: needed by the PUBLIC site
# templates/alpha_engine_tab.html   # un-ignored 2026-08-04: needed by the PUBLIC site
# static/alpha_engine.js   # un-ignored 2026-08-04: needed by the PUBLIC site

# LOCAL-ONLY: Sector auto-mapper cache (NSE TotalMarket → sector names)
# sector_mapper.py   # un-ignored 2026-08-04: needed by the PUBLIC site
.sector_cache.json

# LOCAL-ONLY: sector_indices.py runtime caches (constituents / levels / canonical map)
.sector_indices_cache.json
.sector_levels_cache.json
.sector_canonical_cache.json

# LOCAL-ONLY: ⚔️ Strategy Engine — never pushed to GitHub
strategy_engine.py
templates/strategy_tab.html
.strategy_config.json
.strategy_journal.db
power_plays_validation.py
power_plays_backtest.py
.powerplays_backtest.json
smart_money_validation.py
regime_evidence.py
exit_backtest.py
sector_rotation_evidence.py
sustained_breakout_validation.py
sustained_freshness_validation.py
guardian.db
.watchlist.json

# LOCAL-ONLY: auth secrets — session key + hashed user store. NEVER commit.
.auth_secret
.auth_users.json
.portfolio_hwm.json
